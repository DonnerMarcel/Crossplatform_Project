# pubspec.yaml
name: flutter_application_2 # Your project name
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you intend to publish to pub.dev
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0' # Adjust to your SDK constraints if needed

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  collection: ^1.17.0 # Ensure this version is suitable or update
  google_fonts: ^6.1.0 # Check for latest version on pub.dev
  flutter_riverpod: ^2.5.1 # <-- ADDED (Check pub.dev for the latest stable version)
  firebase_core: ^3.13.0
  cloud_firestore: ^5.6.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # Check for latest version

flutter:
  uses-material-design: true